# Simple File Reader (x86-64 Assembly)

Reads a file specified as a command-line argument and prints its contents to the terminal. Handles errors like missing filename, file not found, or permission denied.

Usage:

`./cat filename.txt`
